repos:
  - repo: https://github.com/psf/black
    rev: 25.1.0
    hooks:
      - id: black
        exclude: "(.venv|venv)/.*"

  - repo: https://gitlab.com/pycqa/flake8
    rev: 7.1.2
    hooks:
      - id: flake8
        exclude: "(.venv|venv)/.*"
